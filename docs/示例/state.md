# 指令代码部分举例
```ini
----------------------------------- 
{($SHIP_MODE == 1) && ($SHIP_PUSHBACK == 0)} 
条件 
----------------------------------- 
#WAIT(90) 
等待 90 秒 
----------------------------------- 
#IAS(240) 
速度 240kt，空速地速都可，括号内为 0 时解除速度限制 
----------------------------------- 
#HANDOFF(APP) 
移交进近 
----------------------------------- 
#SUBCODE(@98,TAXI_REQ_LOOP,1,60) 
在 60 秒后跳转回 TAXI_REQ_LOOP 参数 
----------------------------------- 
#ROUTECHANGETABLE(GP_AP34R_HVISUAL) 
改变航路，需要搭配 route.ini 使用 
----------------------------------- 
#STATE(TAXI_REQ_INIT_DEPARTURE) 
可让飞机跳转到另一个管制参数（状态） 
----------------------------------- 
#EXECUTE(GOAROUND) 
执行复飞
```

```
<TAXI_RUNWAY> 
<TAXI_STAND_BY> 
弹出选项（走行許可、走行待機） 
```